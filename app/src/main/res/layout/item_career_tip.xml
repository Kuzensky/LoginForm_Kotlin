<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="16dp"
    app:cardBackgroundColor="@color/card_bg"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- Top Row: Badge and Icon -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Number Badge -->
            <TextView
                android:id="@+id/txtTipNumber"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/tip_badge_bg"
                android:gravity="center"
                android:fontFamily="@font/poppins_bold"
                android:text="1"
                android:textColor="@color/white"
                android:textSize="20sp" />

            <!-- Icon Circle -->
            <LinearLayout
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginStart="12dp"
                android:background="@drawable/tip_icon_bg"
                android:gravity="center">

                <TextView
                    android:id="@+id/txtTipIcon"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="🎯"
                    android:textSize="24sp" />

            </LinearLayout>

        </LinearLayout>

        <!-- Tip Title -->
        <TextView
            android:id="@+id/txtTipTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:fontFamily="@font/poppins_bold"
            android:text="Set Clear Career Goals"
            android:textColor="@color/white"
            android:textSize="20sp" />

        <!-- Tip Description -->
        <TextView
            android:id="@+id/txtTipDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:fontFamily="@font/poppins"
            android:text="Define specific, measurable, achievable, relevant, and time-bound (SMART) goals for your career path. Having clear objectives helps you stay focused and motivated."
            android:textColor="#B0BEC5"
            android:textSize="15sp"
            android:lineSpacingExtra="4dp" />

    </LinearLayout>

</androidx.cardview.widget.CardView>
